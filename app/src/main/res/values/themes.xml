<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PetGameMidterm" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PetGameMidterm" parent="Base.Theme.PetGameMidterm" />

    <style name="Theme.ThemesAndStyles.Ocean" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/ocean_primary</item>
        <item name="colorSecondary">@color/ocean_accent</item>
        <item name="colorSurface">@color/ocean_bg</item>
        <item name="colorAccent">@color/ocean_progress</item>
    </style>
</resources>